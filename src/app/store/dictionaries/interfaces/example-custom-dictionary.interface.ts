export interface ExampleCustomDictionary {
  // You can place here properties which are different than in default-dictionary.interface.ts (id, name)
  // Next step is use this interface in state interface in generic type and add to type (DictionaryType)
}
